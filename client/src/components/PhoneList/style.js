import styled from "styled-components"

export const PhoneListDiv = styled.div`
    margin-top:110px;
    .flex-container {
      display: flex;
      justify-content: center;
      align-items:center;
      list-style: none;
      -ms-box-orient: horizontal;
      display: -webkit-box;
      display: -moz-box;
      display: -ms-flexbox;
      display: -moz-flex;
      display: -webkit-flex;
    }
    .wrap    { 
      -webkit-flex-wrap: wrap;
      flex-wrap: wrap;
    } 
}`
